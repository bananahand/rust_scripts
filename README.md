# Banana Land Rust Server scripts

These scripts rely on https://github.com/GameServerManagers/LinuxGSM/

version.sh: Checks for updates then runs updates and restarts rust server if an update is detected.
